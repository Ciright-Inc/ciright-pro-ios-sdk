import Foundation

/// SDK-wide constants.
public enum QAConstants {
    /// Default backend base URL.
    public static let defaultBaseURL = "https://api.ciright.pro"

    /// SDK version string.
    public static let sdkVersion = "1.0.0"
}
